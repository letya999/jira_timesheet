[project]
name = "frontend"
version = "0.1.0"
description = "Frontend for Jira Timesheet System"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "streamlit>=1.30.0",
    "pandas",
    "requests",
    "extra-streamlit-components",
    "plotly",
    "openpyxl",
]
