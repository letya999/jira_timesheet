# Implementation Tasks

1. [ ] Create `CardList` component in `web/src/components/shared/card-list.tsx`.
2. [ ] Create `ActionTable` component in `web/src/components/shared/action-table.tsx`.
3. [ ] Create `PivotTable` component in `web/src/components/shared/pivot-table.tsx` (unifying `TimesheetGrid` and `ReportDataTable`).
4. [ ] Refactor `JournalPage` to use `CardList`.
5. [ ] Refactor `DashboardPage` (Recent Activity) to use `CardList`.
6. [ ] Refactor `ProjectsPage` to use `CardList`.
7. [ ] Refactor `ApprovalsPage` to use `CardList`.
8. [ ] Refactor `LeavePage` to use `CardList`.
9. [ ] Refactor `NotificationsPage` to use `CardList`.
10. [ ] Refactor `EmployeesPage` to use `ActionTable`.
11. [ ] Refactor `MyTimesheetPage` to use `PivotTable`.
12. [ ] Refactor `ControlSheet` to use `PivotTable` and `CardList`.
13. [ ] Refactor `ReportsPage` to use `PivotTable`.
14. [ ] Verify all pages work as expected and look consistent.
